# 代理请求端流式转发规范

## 约束

HTTP 代理的请求端和响应端都必须使用流式转发，不得将整个请求/响应体缓冲到内存。

## 验证条件

Given 一个 100MB 的文件上传请求通过网关代理到 DSH 实例

When 请求处理过程中

Then 内存占用不应超过 10MB（流式缓冲区大小），首字节转发延迟不超过 100ms。

Given GET 或 HEAD 请求通过网关代理

When 请求处理过程中

Then 直接调用 `proxyReq.end()`，不读取请求体。
